export async function playWebRTC(
  conf: { room: string; token: string },
  el: HTMLVideoElement
) {
  // TODO: 使用 livekit-client 连接、订阅远端视频，并 attach 到 el
  // 返回 true/false 代表成功/失败
  return false;
}
