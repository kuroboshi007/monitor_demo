export async function playWebRTC(
  conf: { room: string; token: string },
  el: HTMLVideoElement
) {
  // TODO: use livekit-client or similar to play WebRTC stream
  // return true/false
  return false;
}
